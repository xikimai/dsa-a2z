"""
Warmup 1: Square Root (Integer)
=================================
Chapter 16: Binary Search Beyond — When the Answer Is the Question

PROBLEM
-------
Given a non-negative integer n, compute the integer (floor) square root
of n. That is, find the largest integer x such that x * x <= n.

INPUT FORMAT
------------
A single non-negative integer n.

OUTPUT FORMAT
-------------
A single integer — the floor of the square root of n.

CONSTRAINTS
-----------
- 0 <= n <= 10^9

EXAMPLES
--------
Input:
  16
Output: 4

Input:
  8
Output: 2

INSTRUCTIONS
------------
Replace the `pass` in the solve() function with your solution.
The main block at the bottom handles input/output — don't change it.
"""


def solve(n: int) -> int:
    """Return floor of square root of n."""
    pass  # TODO: Replace this with your solution


# ── Do not change anything below this line ──────────────────────────
if __name__ == "__main__":
    n = int(input().strip())
    print(solve(n))
