/*
 * Challenge 1: Reorganize String
 * ================================
 * Chapter 17: Heaps & Priority Queues — The VIP Line
 *
 * PROBLEM:
 *   Given a string s, rearrange the characters so that no two adjacent
 *   characters are the same. Return any valid rearrangement, or ""
 *   if impossible.
 *
 * EXAMPLES:
 *   solve("aab")   -> "aba"  (or any valid arrangement)
 *   solve("aaab")  -> ""     (impossible)
 *
 * CONSTRAINTS:
 *   - 1 <= s.length() <= 500
 *   - s consists of lowercase English letters
 *
 * INSTRUCTIONS:
 *   Replace the body of solve() with your solution.
 */

#include <iostream>
#include <queue>
#include <string>
#include <vector>
using namespace std;

string solve(string s) {
    // TODO: Replace this with your solution
    return "";
}

// -- Do not change anything below this line --------------------------
int main() {
    string s;
    cin >> s;
    cout << solve(s) << endl;
    return 0;
}
