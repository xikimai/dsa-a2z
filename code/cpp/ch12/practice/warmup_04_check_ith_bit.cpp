/*
 * Warmup 4: Check if i-th Bit Is Set
 * Chapter 12: Bit Manipulation — The Language of Computers
 *
 * PROBLEM: Determine if bit i (0-indexed from right) of n is set.
 * EXAMPLES: solve(42,1)=true, solve(42,2)=false
 * CONSTRAINTS: 0 <= n <= 10^9, 0 <= i <= 30
 */

#include <iostream>
using namespace std;

bool solve(int n, int i) {
    // TODO: Replace this with your solution
    return false;
}

int main() {
    int n, i;
    cin >> n >> i;
    cout << (solve(n, i) ? "true" : "false") << endl;
    return 0;
}
