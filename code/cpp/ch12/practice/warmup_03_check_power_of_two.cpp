/*
 * Warmup 3: Check Power of Two
 * Chapter 12: Bit Manipulation — The Language of Computers
 *
 * PROBLEM: Determine if n is a power of two.
 * EXAMPLES: solve(8)=true, solve(6)=false, solve(0)=false
 * CONSTRAINTS: -2^31 <= n <= 2^31 - 1
 */

#include <iostream>
using namespace std;

bool solve(int n) {
    // TODO: Replace this with your solution
    return false;
}

int main() {
    int n;
    cin >> n;
    cout << (solve(n) ? "true" : "false") << endl;
    return 0;
}
