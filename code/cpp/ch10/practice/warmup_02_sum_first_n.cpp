/*
 * Warmup 2: Sum of First N Natural Numbers
 * ==========================================
 * Chapter 10: The Magic of Recursion
 *
 * PROBLEM:
 *   Given a non-negative integer n, compute the sum 1 + 2 + ... + n
 *   recursively. If n == 0, return 0.
 *
 * EXAMPLES:
 *   solve(0)   -> 0
 *   solve(1)   -> 1
 *   solve(5)   -> 15
 *   solve(100) -> 5050
 *
 * CONSTRAINTS:
 *   0 <= n <= 10^4
 *
 * INSTRUCTIONS:
 *   Replace the body with your recursive solution.
 */

#include <iostream>
using namespace std;

int solve(int n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    int n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
