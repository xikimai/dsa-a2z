/*
 * Warmup 1: Factorial
 * ====================
 * Chapter 10: The Magic of Recursion
 *
 * PROBLEM:
 *   Given a non-negative integer n, compute n! (n factorial) recursively.
 *   0! = 1, and n! = n * (n-1)! for n > 0.
 *
 * EXAMPLES:
 *   solve(0)  -> 1
 *   solve(1)  -> 1
 *   solve(5)  -> 120
 *   solve(10) -> 3628800
 *
 * CONSTRAINTS:
 *   0 <= n <= 20
 *   Use long long to avoid overflow (13! exceeds int range).
 *
 * INSTRUCTIONS:
 *   Replace the body with your recursive solution.
 */

#include <iostream>
using namespace std;

long long solve(int n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    int n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
