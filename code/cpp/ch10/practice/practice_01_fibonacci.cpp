/*
 * Practice 1: Fibonacci Number
 * ==============================
 * Chapter 10: The Magic of Recursion
 *
 * PROBLEM:
 *   Given a non-negative integer n, return the nth Fibonacci number.
 *   F(0) = 0, F(1) = 1, F(n) = F(n-1) + F(n-2) for n >= 2.
 *
 * EXAMPLES:
 *   solve(0)  -> 0
 *   solve(1)  -> 1
 *   solve(10) -> 55
 *   solve(15) -> 610
 *
 * CONSTRAINTS:
 *   0 <= n <= 30
 *
 * INSTRUCTIONS:
 *   Replace the body with your solution. You may use memoization.
 */

#include <iostream>
using namespace std;

int solve(int n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    int n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
