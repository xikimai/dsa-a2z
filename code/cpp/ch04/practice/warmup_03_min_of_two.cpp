/*
 * Warmup 3: Min of Three
 * ======================
 * Chapter 4: Functions
 *
 * PROBLEM:
 *   Find the minimum of three integers. You MUST write a helper function
 *   called min_of_two(int a, int b) and use it to build the solution.
 *   Do NOT use the built-in min() function.
 *
 * EXAMPLES:
 *   solve(3, 1, 2)    -> 1
 *   solve(5, 5, 5)    -> 5
 *   solve(-1, 0, 1)   -> -1
 *   solve(10, 3, 7)   -> 3
 *
 * CONSTRAINTS:
 *   - All integers fit in int range
 */

#include <iostream>
using namespace std;

// TODO: Write a helper function min_of_two(int a, int b)

/**
 * Returns the minimum of three integers using min_of_two helper.
 */
int solve(int a, int b, int c) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    int a, b, c;
    cin >> a >> b >> c;
    cout << solve(a, b, c) << endl;
    return 0;
}
