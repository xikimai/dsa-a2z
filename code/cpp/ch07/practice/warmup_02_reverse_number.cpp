/*
 * Warmup 2: Reverse Number
 * ========================
 * Chapter 7: Number Wizardry -- Math for Programmers
 *
 * PROBLEM:
 *   Given integer n, return the number with its digits reversed.
 *   Preserve the sign: -123 -> -321.
 *   Trailing zeros disappear: 1200 -> 21.
 *
 * EXAMPLES:
 *   solve(12345) -> 54321
 *   solve(-123) -> -321
 *   solve(1200) -> 21
 *   solve(0) -> 0
 *
 * CONSTRAINTS:
 *   -10^18 <= n <= 10^18
 *
 * INSTRUCTIONS:
 *   Replace "return 0;" with your solution.
 *   Build the reversed number digit by digit using mod-10/div-10.
 */

#include <cstdlib>
#include <iostream>
using namespace std;

long long solve(long long n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    long long n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
