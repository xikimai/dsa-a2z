/*
 * Warmup 3: Sum of Digits
 * =======================
 * Chapter 7: Number Wizardry -- Math for Programmers
 *
 * PROBLEM:
 *   Given integer n, return the sum of its digits. Use abs(n).
 *   0 has a digit sum of 0.
 *
 * EXAMPLES:
 *   solve(12345) -> 15
 *   solve(0) -> 0
 *   solve(-456) -> 15
 *   solve(999) -> 27
 *
 * CONSTRAINTS:
 *   -10^18 <= n <= 10^18
 *
 * INSTRUCTIONS:
 *   Replace "return 0;" with your solution.
 *   Use the mod-10/div-10 pattern to extract and sum each digit.
 */

#include <cstdlib>
#include <iostream>
using namespace std;

int solve(long long n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    long long n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
