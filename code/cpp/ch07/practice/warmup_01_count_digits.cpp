/*
 * Warmup 1: Count Digits
 * =========================
 * Chapter 7: Number Wizardry -- Math for Programmers
 *
 * PROBLEM:
 *   Given integer n, return the number of digits. Use abs(n). 0 has 1 digit.
 *
 * EXAMPLES:
 *   solve(12345) -> 5
 *   solve(0) -> 1
 *   solve(-42) -> 2
 *
 * CONSTRAINTS:
 *   -10^18 <= n <= 10^18
 *
 * INSTRUCTIONS:
 *   Replace "return 0;" with your solution.
 *   Do NOT convert to a string -- use the mod-10/div-10 pattern!
 */

#include <cstdlib>
#include <iostream>
using namespace std;

int solve(long long n) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    long long n;
    cin >> n;
    cout << solve(n) << endl;
    return 0;
}
