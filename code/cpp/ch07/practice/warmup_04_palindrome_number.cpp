/*
 * Warmup 4: Palindrome Number
 * ===========================
 * Chapter 7: Number Wizardry -- Math for Programmers
 *
 * PROBLEM:
 *   Given integer n, return true if it is a palindrome (reads the
 *   same forwards and backwards). Negative numbers are NOT palindromes.
 *
 * EXAMPLES:
 *   solve(121) -> true
 *   solve(-121) -> false
 *   solve(10) -> false
 *   solve(0) -> true
 *   solve(1234321) -> true
 *
 * CONSTRAINTS:
 *   -10^18 <= n <= 10^18
 *
 * INSTRUCTIONS:
 *   Replace "return false;" with your solution.
 *   Reverse the number and compare with the original.
 */

#include <iostream>
using namespace std;

bool solve(long long n) {
    // TODO: Replace this with your solution
    return false;
}

// -- Do not change anything below this line --------------------------
int main() {
    long long n;
    cin >> n;
    cout << (solve(n) ? "true" : "false") << endl;
    return 0;
}
