/*
 * Warmup 3: Count Vowels
 * ======================
 * Chapter 5: Collections
 *
 * PROBLEM:
 *   Count the number of vowels (a, e, i, o, u) in a string.
 *   The check should be case-insensitive.
 *
 * EXAMPLES:
 *   solve("Hello World") -> 3
 *   solve("aeiou")       -> 5
 *   solve("xyz")         -> 0
 *   solve("")            -> 0
 *
 * CONSTRAINTS:
 *   - 0 <= s.length() <= 10^5
 *   - s contains printable ASCII characters
 */

#include <iostream>
#include <string>
using namespace std;

/**
 * Returns the number of vowels in s (case-insensitive).
 */
int solve(string s) {
    // TODO: Replace this with your solution
    return 0;
}

// -- Do not change anything below this line --------------------------
int main() {
    string s;
    getline(cin, s);
    cout << solve(s) << endl;
    return 0;
}
